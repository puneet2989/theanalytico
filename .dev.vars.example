TURNSTILE_SECRET_KEY=
SMTP_USER=
SMTP_PASS=

# Optional. Uncomment to override the default.
# SMTP_HOST=          (default: smtp.purelymail.com)
# SMTP_PORT=          (default: 465)
# MAIL_TO=            (default: same as SMTP_USER)
# RATE_LIMIT=         (KV namespace binding; unbound = rate limiting silently skipped)
